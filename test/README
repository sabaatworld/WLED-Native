
This directory contains repository tests that run through Node.js (`node --test`).

Current coverage includes the web asset build helpers and the native host
runtime smoke/integration checks. Keep new tests wired into `npm test` rather
than adding a separate side runner.
